// As quatro classes-base: Veterano (Cosmonauta), Operativo (Gatuno),
// Técnico (Cientista) e Sensível à Força (Mentálico), do 1º ao 20º nível.
//
// Cada classe traz a PRÓPRIA tabela de níveis (`levels`), como no livro: no
// Space Dragon não há fórmula única de XP, e a progressão por chassi do Star
// Dragon (progressoes.mjs) era do Old Dragon 2 — por isso não veio.
//
// Fonte: o cofre, em Documents\Ekhoria\20 Space Dragon\Space Dragon Suplemento\
//   SW-SUP-Classes.md (as três mundanas) e SW-SUP-Forca.md (o Sensível)
//
// ⚠️ AINDA NÃO TRANSCRITO. O export existe vazio para o build já rodar e o
// compêndio já existir no Foundry; o conteúdo entra nota por nota.

export const classes = [];
